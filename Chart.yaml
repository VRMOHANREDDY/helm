apiVersion: v2                    # Helm chart API version; v2 = Helm 3
name: nginx                       # Chart name, used to identify/install the chart
version: 0.1.3                    # Chart version; bump it whenever the chart itself changes
description: upgrading to 1.31.3-trixie  # Short human-readable note about this chart/release
appVersion: 1.31.3-trixie        # Version of the app inside (nginx image tag), separate from chart version